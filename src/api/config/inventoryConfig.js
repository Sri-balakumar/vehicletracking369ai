// src/api/config/inventoryConfig.js
// Central config for inventory-related API base URL.
// Inventory API base URL (force to production/test server IP to avoid ngrok tunnels)
const INVENTORY_API_BASE = 'https://beatles-aware-aware-receivers.trycloudflare.com';

export default INVENTORY_API_BASE;
